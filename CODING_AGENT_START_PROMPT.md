# Start prompt for the coding agent

Read `LOYALTY_SAAS_IMPLEMENTATION_BRIEF.md` in this workspace in full before implementing. Treat it as the launch-only product specification, including Phases 0–9 and their acceptance criteria. Implement the launch requirements without adding a future-version roadmap or speculative infrastructure.

Build the Pakistani cafe and restaurant loyalty SaaS it describes: one shared installable PWA, branded business cards, stamp/points programmes, staff scanning and secure redemptions, referral rewards, scheduled double-stamp slots, owner reporting, automated push campaigns, and MANUAL WhatsApp click-to-chat follow-ups. Referral links, double-stamp scheduling, and owner reporting are required at launch.

Use the specified Next.js/TypeScript, Supabase/PostgreSQL/Auth/Storage, FCM, and PostgreSQL-backed worker architecture unless the repository audit establishes a concrete incompatibility. Verify current supported stable dependency versions, pin them, and document your choices. Do not introduce separate native apps, unofficial WhatsApp automation, Meta API sending, or background PWA geofencing.

Start with Phase 0: inspect existing code and applicable instructions, preserve unrelated work, record setup constraints, create the implementation checklist, and establish a runnable development environment. Then implement phases in order. Maintain `docs/implementation-status.md` with evidence and outstanding work. Continue routine authorized implementation without asking for approval after every phase.

Prioritize real persistence, tenant isolation, transactional ledger correctness, fast mobile checkout, accessible English-only interfaces, and honest notification/reporting states. Do not add language selection, translated content variants, or a translation framework. Do not deliver mock-only features as complete. Use narrowly scoped database functions and test direct API/RPC access as well as the UI. Test concurrency, idempotency, referral qualification/reversal, promotion boundaries, consent changes, and shared-device push handling.

Sections 24 and 25 define the database fields/relationships and page-by-page UI contracts. Implement those specifications; do not guess product fields, accounting rules, or permissions. You own SQL migrations, indexes, transactional functions, generated types, component implementation, and validation tests. Document any necessary structural refinement while preserving these contracts.

Use the audited version 1.6 contracts: section 4's exact permissions/MFA, section 7's qualification/discount/locking rules, section 9's exact referral encoding/unavailable behavior, section 10's push text limits and zero-recipient behavior, section 11's WhatsApp placeholder rules, section 13's API payloads, section 17's explicit Vercel ingress trust and privacy/export limits, and section 18's payment transitions. Preserve birthday instances and push registration/dispatch rules. Section 24 includes supporting records and matching validation bounds; section 25 defines 43 screen contracts including bounded S04 activity pagination. Cross-check every field, operation, table, and permission; include the explicit regression cases in meaningful tests.

Implement section 26's simple caching policy: built-in CDN caching for versioned public assets and sanitized public information with bounded TTLs. Reports query indexed PostgreSQL source tables on demand, with current permission checks, date limits, pagination, timeout handling, and a Refresh button. Do not create report cache tables, report-refresh jobs, or incremental report aggregates. Keep pg-boss for agreed business jobs; add no Redis or Inngest. Keep private responses no-store and preserve logout/account-switch isolation.

Deploy the web application to Vercel as specified in section 27. Keep pg-boss on a separately hosted persistent Node worker; do not launch it inside Vercel request functions. Use the provisional Singapore function/database placement, benchmark against matched Mumbai placement from Pakistani networks before production selection, and document the final configuration. Use Vercel's global CDN without inventing a Pakistan CDN region. Preserve production/preview isolation and section 26's simple TTL policy.

Where credentials or external services are unavailable, implement and test the local boundary, clearly identify the remaining staging/production verification, and continue independent work. Never invent credentials, send live campaigns to real customers during development, or claim a mock proves real-device delivery.

Section 27.5 separates real owner/provider inputs from engineering decisions and defines release evidence. Implement independently using the specified behavioral defaults; leave missing real identity, pricing, policy content, payment accounts, credentials, and provider/device verification as explicit configuration gates. Validate direct private-storage uploads before public asset publication, bounded private export downloads, and a real early auth/RPC/worker/push integration slice. No application is considered built or production-ready merely because this specification or a mock is complete.

At each milestone, report what is implemented, how it was checked, any material unresolved limitation, and the next phase. The final handoff must include setup, migrations, seed data, test evidence, deployment/runbooks, and an accurate launch-readiness checklist.
